//* elimine los espacios de la cadena y luego devuelva la cadena resultante.

const replaceSpace = (str) => {
	return str.replace(/\s/g, '');
};

console.log(replaceSpace('8 j 8   mBliB8g  imjB8B8  jl  B'));
