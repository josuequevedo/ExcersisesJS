// Write a function that takes an array of numbers as argument
// It should return an array with the numbers sorted in descending order

const order = (arr) => {
	return arr.sort();
};

console.log(order([8, 4, 6, 1, 2]));
